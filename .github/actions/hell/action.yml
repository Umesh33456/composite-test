name: "Hello Composite"
description: "Simple composite workflow test"

inputs:
  name:
    description: "Person name"
    required: true

runs:
  using: "composite"
  steps:
    - name: Say Hello
      shell: bash
      run: echo "Hello ${{ inputs.name }} from Composite Action!"
