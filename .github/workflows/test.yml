name: Test Composite

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run Composite
        uses: ./.github/actions/hello
        with:
          name: Umesh
